import { Typography } from "@mui/material";
import React from "react";

export default function NotFoundPage() {
    return (
        <React.Fragment>
            <Typography textAlign="center" sx={{mt: 8, fontWeight: 'bold'}}>What are you doing here? Go back!</Typography>
        </React.Fragment>
    );
}